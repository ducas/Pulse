@model Web.Models.BusinessEntities.ListItem
<script type="text/x-kendo-tmpl" id="busineEntitiesList_itemTemplate">
<li class="business-entity">
    @*<a href="@Url.Action("View")/${@Html.NameFor(m => m.Id)}" class="transition">*@
        <h3>${@Html.NameFor(m => m.Name)}</h3>
        <span>${@Html.NameFor(m => m.ShortDescription)}</span>
        <span class="selected-visible">
            <span>${@Html.NameFor(m => m.LongDescription)}</span>
            <span class="actions">
                <a href="@Url.Action("View")/${@Html.NameFor(m => m.Id)}" class="transition">View</a> | 
                <a href="@Url.Action("Edit")/${@Html.NameFor(m => m.Id)}" class="transition">Edit</a> | 
                <a href="@Url.Action("Delete")/${@Html.NameFor(m => m.Id)}" class="delete">Delete</a>
            </span>
        </span>
    @*</a>*@
</li>
</script>